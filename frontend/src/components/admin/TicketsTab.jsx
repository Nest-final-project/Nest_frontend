import React from 'react';
import TicketManagement from './TicketManagement';

const TicketsTab = ({ isDarkMode }) => {
  return <TicketManagement isDarkMode={isDarkMode} />;
};

export default TicketsTab;