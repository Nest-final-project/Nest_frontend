import React from 'react';
import CouponManagement from './CouponManagement';

const CouponsTab = ({ isDarkMode }) => {
  return <CouponManagement isDarkMode={isDarkMode} />;
};

export default CouponsTab;